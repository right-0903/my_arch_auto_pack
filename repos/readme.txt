package-name
├── host: host by gitlab or aur (or github)
├── url: place the repo url of this package without .git suffix
├── version: place the version of this package
└── quirks: place the patch/scipt to do with this package(TODO)

I enumrate every package, to determine if they have an update,
